[advisories]
# A list of advisory IDs to ignore. Note that ignored advisories will still
# output a note when they are encountered.

ignore = [
    # `paste` is unmaintained; pulled in transitively (no vulnerability, proc-macro only)
    "RUSTSEC-2024-0436",
    # `proc-macro-error2` is unmaintained; pulled in via alloy-sol-macro (no vulnerability,
    # proc-macro only). Upstream alloy-rs/core already migrated to proc-macro-error3 on main —
    # drop this once a release after alloy-core 1.6.0 ships and we update.
    "RUSTSEC-2026-0173",
]

[licenses]
confidence-threshold = 0.9
# List of explicitly allowed licenses
# See https://spdx.org/licenses/ for list of possible licenses
# [possible values: any SPDX 3.11 short identifier (+ optional exception)].
allow = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "BSD-3-Clause",
    "BSD-2-Clause",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    "GPL-3.0-or-later",
    "CC0-1.0",
    "CDLA-Permissive-2.0",
    "Unlicense",
    "bzip2-1.0.6",
]

# The subxt crates declare `Apache-2.0 OR GPL-3.0`, where `GPL-3.0` is a deprecated
# SPDX identifier that makes cargo-deny emit a parse warning (still unfixed upstream
# as of subxt 0.50.1). Clarify them all to the modern equivalent expression.
[[licenses.clarify]]
crate = "subxt"
expression = "Apache-2.0 OR GPL-3.0-only"
license-files = []

[[licenses.clarify]]
crate = "subxt-codegen"
expression = "Apache-2.0 OR GPL-3.0-only"
license-files = []

[[licenses.clarify]]
crate = "subxt-core"
expression = "Apache-2.0 OR GPL-3.0-only"
license-files = []

[[licenses.clarify]]
crate = "subxt-macro"
expression = "Apache-2.0 OR GPL-3.0-only"
license-files = []

[[licenses.clarify]]
crate = "subxt-metadata"
expression = "Apache-2.0 OR GPL-3.0-only"
license-files = []

[[licenses.clarify]]
crate = "subxt-rpcs"
expression = "Apache-2.0 OR GPL-3.0-only"
license-files = []

[[licenses.clarify]]
crate = "subxt-signer"
expression = "Apache-2.0 OR GPL-3.0-only"
license-files = []

[[licenses.clarify]]
crate = "subxt-utils-fetchmetadata"
expression = "Apache-2.0 OR GPL-3.0-only"
license-files = []

[bans]
# Lint level for when multiple versions of the same crate are detected
multiple-versions = "warn"
# The graph highlighting used when creating dotgraphs for crates
# with multiple versions
# * lowest-version - The path to the lowest versioned duplicate is highlighted
# * simplest-path - The path to the version with the fewest edges is highlighted
# * all - Both lowest-version and simplest-path are used
highlight = "all"

[sources]
# Lint level for what to happen when a crate from a crate registry that is not
# in the allow list is encountered
unknown-registry = "deny"
# Lint level for what to happen when a crate from a git repository that is not
# in the allow list is encountered
unknown-git = "deny"
# List of URLs for allowed crate registries. Defaults to the crates.io index
# if not specified. If it is specified but empty, no registries are allowed.
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
# List of URLs for allowed Git repositories
allow-git = []

[sources.allow-org]
# github.com organizations to allow git sources for
github = []
# gitlab.com organizations to allow git sources for
gitlab = []
# bitbucket.org organizations to allow git sources for
bitbucket = []
